# Documentation build dependencies.
# Top-level pins only; Sphinx resolves compatible transitive packages
# (sphinxcontrib-*, Jinja2, Pygments, docutils, requests, ...).

sphinx==9.1.0
sphinx-book-theme==1.2.0
pydata-sphinx-theme==0.16.1
sphinx-tabs==3.5.0

# dev (local live-reload via `yarn docs:serve`)
sphinx-autobuild
